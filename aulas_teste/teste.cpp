#include <bits/stdc++.h>
#include <iostream>

using namespace std;

int main() {

    cout << 1e3;
    // cout << "Hello, world!" << endl;
    // cout << "Eu te amo bibibibibi\n\n";

    // auto x = 0, y = 0;
    // cin >> x >> y;

    // auto max_number = max(x, y);
    // auto min_number = min(x, y);

    // cout << "Max number: " << max_number << endl;
    // cout << "Min number: " << min_number << endl;

    return 0;
}